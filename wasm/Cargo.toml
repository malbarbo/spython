[package]
name = "wasm"
version = "0.1.0"
edition = "2024"

[lib]
name = "spython"
crate-type = ["cdylib"]

[dependencies]
engine = { path = "../engine" }

[profile.release]
opt-level = 's'
lto = true
codegen-units = 1
strip = true
panic = "abort"
